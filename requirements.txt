# =============================================================================
# Keep versions in sync with pyproject.toml:
#   [project.dependencies]  → block "Runtime"
#   [project.optional-dependencies.dev] → block "Dev"
# =============================================================================

# --- Runtime ---
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
requests>=2.28
xgboost>=2.0

# --- Dev (tests / coverage) ---
pytest>=7.0
pytest-cov>=4.0

# --- Install this repository as an editable package ---
# Enables: import feature_engineering, model_training, data_processing, orie5270_ozone
-e .
